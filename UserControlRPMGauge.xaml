<UserControl x:Class="Gauge.UserControlRPMGauge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gauge"
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="800">
    <Grid>
        <Image Source="./images/rpm.png" Width="600" HorizontalAlignment="Center" VerticalAlignment="Top">
        </Image>
        <Slider Minimum="-213" Maximum="78" Width="600" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="100,0,100,100" Value="{Binding Angle}"/>
        <Border Width="5" CornerRadius="120 120 0 0" Background="Blue" RenderTransformOrigin="0.5 1" Height="260" Margin="0,0,0,460" 
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="{Binding Angle}"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Border.RenderTransform>
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" Color="Azure" ShadowDepth="2"/>
            </Border.Effect>
        </Border>
    </Grid>
</UserControl>
